generator client {
  provider = "prisma-client-js"
  // output   = ""
  // output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Order {
  id            String   @id @unique
  creacreatedAt DateTime
  valueTotal    Float

  @@map("orders")
}
